/*#include "CurieIMU.h"

class CapteurMouvement {
  private:
    int countUntilShake;
    unsigned long interruptsTime;    // get the time when motion event is detected
  public:
    CapteurMouvement();
    int getCountUntilShake();
    void setCountUntilShake(int val);
    static void eventCallback(void);
};*/
